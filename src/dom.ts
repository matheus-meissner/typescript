const campoNome = document.getElementById('nome'); // HTMLElement
const formulario = document.getElementsByTagName('form'); // HTMLFormElement
const botaoEnviar = document.getElementById('btn-enviar'); // HTMLElement
botaoEnviar?.addEventListener('click', (e: MouseEvent) => {}) // F12 -- acessa a documentação das propriedades da linguagem (MouseEvent);