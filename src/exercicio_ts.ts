// Função de multiplicação
const multiplica = (numero1: number, numero2: number): number => numero1 * numero2
let numero1: number = 5;
let numero2: number = 2;


// Função de saudação
const saudacao = (nome1: string): string => `Olá ${nome1}`;
let nome1: string = 'Matheus';

console.log(multiplica(numero1, numero2));
console.log(saudacao(nome1));